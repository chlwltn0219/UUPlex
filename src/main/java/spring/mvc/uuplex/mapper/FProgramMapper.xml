<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<!-- 피트니스 프로그램 관련 -->
<mapper namespace="spring.mvc.uuplex.fitness.dao.FProgramDAO" >

	<!-- 피트니스 프로그램 목록 총 수 -->
	<select id="programCount" resultType="int">
		SELECT COUNT(*) FROM F_PROGRAM
	</select>
	
	<!-- 피트니스 종목 프로그램 리스트 반환 -->
	<select id="programList" parameterType="Map" resultType="spring.mvc.uuplex.fitness.dto.FProgramDTO">
		SELECT *
		  FROM ( SELECT p.* , rownum AS rnum 
				   FROM ( SELECT pid, pname, 
				   				(SELECT sname FROM F_SPORTS s WHERE s.sid=i.sid) AS sname, 
				   				(SELECT tname FROM F_TEACHER t WHERE t.tid=i.tid) AS tname, 
				   				activated 
			  		     	FROM F_PROGRAM i
			  			   ORDER BY activated DESC, pid DESC ) p )
		 WHERE rnum BETWEEN #{start} AND #{end}
	</select>
	
	<!-- 피트니스 종목 추가 -->
	<insert id="addSports" parameterType="spring.mvc.uuplex.fitness.dto.FSportsDTO">
		INSERT INTO F_SPORTS (sid, sname, sinfo)
		VALUES (SEQ_SPORTS_SID.nextval, #{sname}, #{sinfo})
	</insert>
	
	<!-- 종목 상세 정보 -->
	<select id="sportsDetail" resultType="spring.mvc.uuplex.fitness.dto.FSportsDTO" parameterType="int">
		SELECT * 
		 FROM F_SPORTS
		WHERE sid=#{sid}
	</select>
	
	<!-- 종목 정보 수정 -->
	<update id="modifySports" parameterType="spring.mvc.uuplex.fitness.dto.FSportsDTO">
		UPDATE F_SPORTS
		   SET sname = #{sname},
			   sinfo = #{sinfo},
			   activated = #{activated}
		 WHERE sid = #{sid}
	</update>
	
</mapper>